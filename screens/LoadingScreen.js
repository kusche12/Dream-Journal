import * as React from 'react';
import { SafeAreaView, Text } from 'react-native';

const LoadingScreen = () => {
    return (
        <SafeAreaView>
            <Text>We are currently loading your dreams...</Text>
        </SafeAreaView>
    );
}

export default LoadingScreen;